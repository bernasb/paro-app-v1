# Testing Paro App

This folder is for unit and integration tests.

- Place all `.test.ts` or `.spec.ts` files here or in appropriate subdirectories.
- Use your preferred test runner (e.g., Jest, Vitest) and update the scripts in `package.json` accordingly.

## Example Test Command

```
npm test
```
